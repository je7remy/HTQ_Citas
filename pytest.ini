[pytest]
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    requires_weasyprint: requiere WeasyPrint real (renderiza PDF con %%EOF y tamaño realista); excluir con -m "not requires_weasyprint" cuando se corre con el stub local
