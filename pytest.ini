[pytest]
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    requires_weasyprint: requiere WeasyPrint real (renderizado de texto en el PDF); excluir con -m "not requires_weasyprint" cuando se use el stub local
